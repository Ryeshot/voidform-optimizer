name: Deployment
on: 
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
jobs:
  Deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Build and Deploy
        run: |
          echo "Deployment started"
          node ./script.js
          echo "Deployment finished"
