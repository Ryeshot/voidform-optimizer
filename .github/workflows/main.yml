name: Deployment
on: 
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
jobs:
  Deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Build and Deploy
        run: |
          echo "Deployment started"
          npm run deploy
          echo "Deployment finished"
